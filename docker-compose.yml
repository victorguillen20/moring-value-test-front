services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: torneo-galactico-frontend
    ports:
      - "4200:80"
    environment:
      API_URL: http://localhost:8080/api
    restart: on-failure
